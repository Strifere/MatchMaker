<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".domain.controllers.DomainController"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeneradorDeEmparejamientos">
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.NamesActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.RoundsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.TableActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ClassificationActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.TournamentActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.PlayersActivity"
            android:exported="false" />
    </application>

</manifest>